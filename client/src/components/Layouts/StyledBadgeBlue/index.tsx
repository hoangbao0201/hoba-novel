import classNames from 'classnames/bind'
import styles from './StyledBadgeBlue.module.scss';
const cx = classNames.bind(styles);

export interface StyledBadgeBlueProps {

}
const StyledBadgeBlue = () => {

    return (
       <div className={cx("badgeblue")}>
            
       </div>
   )
}

export default StyledBadgeBlue